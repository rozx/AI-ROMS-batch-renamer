export interface RomData {
	chineseTitle: string;
	title: string;
	platform: string;
	year: string;
	version: string;
	region: string;
}

export interface RomRenameHistory {
	originalName: string;
	newName: string;
}
